# 🔧 إصلاح سريع - Quick Fix

## المشكلة: httptools يحتاج python3-dev

### الحل السريع:

```bash
cd /home/ai/ai-agent/backend

# تثبيت python3-dev
sudo apt-get install -y python3-dev build-essential

# ثم تثبيت dependencies
./install_dependencies.sh
```

### أو استخدم السكربت الجاهز:

```bash
cd /home/ai/ai-agent/backend
./fix_dependencies.sh
```

---

## بعد الإصلاح:

```bash
# تشغيل الباك إند
./start_backend.sh
```

---

## ملاحظة:

تم تحديث `requirements.txt` لإزالة `uvicorn[standard]` واستخدام `uvicorn` العادي فقط (لا يحتاج httptools).

إذا أردت الأداء الأفضل، يمكنك تثبيت python3-dev ثم إعادة تثبيت uvicorn[standard].

